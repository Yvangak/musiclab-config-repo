
Spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ARTISTSDB?createDatabaseIfNotExist=true
    username: root
    password: password
    platform: mysql
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create
    generate-ddl: true
    show-sql: true


external:
  services:
    songs: musiclab-songs
    version: 2021-01
